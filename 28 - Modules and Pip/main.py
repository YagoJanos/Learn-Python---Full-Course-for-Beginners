import userful_tools

# Now, a module is essentialy just a python file that we can import
# into our current python file

print(userful_tools.roll_dice(10))

# We can search for a huge list of python modules in the web, just search for "List of python modules"